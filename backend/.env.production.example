# Banco de dados — Prisma (necessário para migrations)
DATABASE_URL=postgresql://qa_user:qa_password@db:5432/qa_control

# pg pool (mantido para compatibilidade com as queries existentes)
DB_HOST=db
DB_PORT=5432
DB_DATABASE=qa_control
DB_USER=qa_user
DB_PASSWORD=qa_password

# JWT — gerado com: node -e "require('crypto').randomBytes(32).toString('hex')"
JWT_SECRET=53cc40428b81cc05b60d78a2793a918ea465ed1f0d546905
JWT_EXPIRES_IN=8h

# Token para cadastro de novos usuários
REGISTER_TOKEN=53cc40428b81cc05b60d78a2793a918ea465ed1f0d546905

# Bcrypt rounds (opcional — padrão: 12)
# BCRYPT_ROUNDS=12
