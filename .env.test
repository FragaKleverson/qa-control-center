# Banco de teste — NUNCA altere DB_DATABASE aqui para o banco de produção
# Este arquivo é usado como fallback; as variáveis reais ficam em backend/.env.test
DB_USER=qauser
DB_PASSWORD=qapass
DB_DATABASE=qa_control_test
DB_HOST=localhost
DB_PORT=5434
